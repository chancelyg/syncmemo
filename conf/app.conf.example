[general]
SITE_NAME = syncmemo

[memo]
# 便签大小限制
MEMO_MAX_SIZE = 5
# 便签保存间隔(毫秒)
SAVE_SPANTIME = 5000
# 默认便签ID长度
MEMO_ID_LENGTH = 4
# 用户输入便签最大长度（MB）
MEMO_ID_MAX_LENGTH = 25

[store]
PATH = data/cache
VALID_DAYS = 30